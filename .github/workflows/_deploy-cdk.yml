name: Deploy CDK

on:
  workflow_call:
    inputs:
      action:
        description: 'CDK action (diff, deploy, destroy)'
        required: false
        type: string
        default: 'deploy'
      environment:
        description: 'Environment (staging, production)'
        required: false
        type: string
        default: 'staging'

env:
  AWS_REGION: us-west-2

jobs:
  cdk:
    name: CDK ${{ inputs.action }} (${{ inputs.environment }})
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build infrastructure
        run: pnpm --filter @doc-platform/infra build

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ inputs.environment == 'production' && secrets.AWS_DEPLOY_ROLE_ARN_PRODUCTION || secrets.AWS_DEPLOY_ROLE_ARN }}
          aws-region: ${{ env.AWS_REGION }}

      - name: CDK ${{ inputs.action }}
        run: |
          cd infra
          case "${{ inputs.action }}" in
            diff)
              npx cdk diff --context env=${{ inputs.environment }}
              ;;
            deploy)
              npx cdk deploy --require-approval never --context env=${{ inputs.environment }}
              ;;
            destroy)
              npx cdk destroy --force --context env=${{ inputs.environment }}
              ;;
            *)
              echo "Unknown action: ${{ inputs.action }}"
              exit 1
              ;;
          esac

      - name: Output Stack Info
        if: inputs.action == 'deploy'
        run: |
          if [ "${{ inputs.environment }}" = "staging" ]; then
            STACK_NAME="DocPlatformStack"
          else
            STACK_NAME="DocPlatform-${{ inputs.environment }}"
          fi
          aws cloudformation describe-stacks \
            --stack-name "$STACK_NAME" \
            --query "Stacks[0].Outputs" \
            --output table \
            --region ${{ env.AWS_REGION }}
